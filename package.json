{
  "name": "text-to-reality-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:functions": "node scripts/build-functions.mjs",
    "deploy": "npm run build:functions && wrangler pages deploy public",
    "setup": "node scripts/setup.js"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
