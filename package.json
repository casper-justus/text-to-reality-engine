{
  "name": "text-to-reality-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public",
    "setup": "node scripts/setup.js"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
